var e={meta:{slug:"dark-mode-as-design-system",title:"Dark Mode Is a Design System, Not a CSS Toggle",excerpt:"Every product I've shipped a light/dark toggle for has taught me the same lesson: it's a semantic-token design problem, and it's decided before anyone writes CSS.",tag:"UI/UX Design",readTime:"6 min",publishedLabel:"Field notes"},intro:"On Compito, I built a light/dark mode toggle backed by a Local Storage service so the theme choice persists across sessions. Shipping the toggle itself was the easy part — a service and a stored preference. The actual design work happened weeks earlier, in Figma, deciding how the entire color system would behave in both modes before a single dark-mode class existed in the codebase.",sections:[{heading:"The retrofit is always more expensive than designing it in",paragraphs:["The difference between a five-minute dark-mode toggle and a multi-day retrofit is entirely about when the semantic token layer gets designed. If components reference raw hex values — a common shortcut under deadline pressure — dark mode means finding and re-deciding every one of those values after the fact, screen by screen, with no guarantee of consistency between them.","Designed in from the start, dark mode is just a second definition of the same token names: --bg-surface, --text-heading, --border-focus get a light value and a dark value, and every component that only ever referenced the token name picks up the right one automatically. The component library doesn't know or care which mode is active — that's the entire point of the abstraction."]},{heading:"Contrast doesn't just invert — it has to be redesigned per mode",paragraphs:["The naive version of dark mode is inverting lightness values and calling it done. It doesn't hold up: a saturated accent color that reads clearly on white can vibrate uncomfortably on near-black, and a subtle border that's barely visible in light mode can disappear entirely in dark mode where the surface-to-border contrast ratio is different.","Every semantic color pairing gets checked independently in both modes against WCAG contrast minimums, not assumed to inherit correctness from its light-mode counterpart. On status badges and coverage indicators — the kind of element Rota's roster board depends on for fast scanning — I specifically re-tune saturation and lightness per mode rather than trusting a single hue value to work everywhere."],bullets:["Every token gets an explicit dark-mode value — never an automatic invert or opacity trick","Recheck contrast ratios independently per mode; a pairing that passes in light mode can fail in dark","Test the busiest, most saturated screen in dark mode first — status badges, charts, coverage indicators — not a quiet settings page"]},{heading:"Dark mode changes what 'elevation' means",paragraphs:["In light mode, elevation (a card above a background) is usually communicated with shadow. In dark mode, shadows read poorly against a dark background, and the convention shifts to subtle background-lightness steps instead — a surface a level higher gets a slightly lighter dark tone, not a heavier shadow. Designing this as its own token axis (surface, surface-alt, surface-raised) rather than trying to reuse the light-mode shadow system was what made elevation still legible once dark mode shipped."]},{heading:"What I check before calling a dark theme done",paragraphs:["I walk every screen state — not just the happy path, but empty, loading and error states too — in both modes before sign-off, because those are exactly the states most likely to have been designed once, in light mode, and never revisited. A dark-mode empty-state illustration that was never re-checked is a common gap, and it's always on the screen someone hits during an actual problem."]}],takeaway:"Dark mode is decided in the token design pass, not in the CSS implementation pass — treat it as a second, equally deliberate palette from day one, and the toggle itself becomes trivial engineering instead of a redesign in disguise."};export{e as darkModeAsDesignSystem};
