var e={meta:{slug:"scss-architecture-that-survives-teams",title:"A 7-1 SCSS Architecture That Survives Multiple Teams and Multiple Years",excerpt:"The stylesheet structure I've rebuilt on three separate enterprise apps now, and why the boring, disciplined version wins over the clever one every time.",tag:"Architecture",readTime:"6 min",publishedLabel:"Field notes"},intro:"I've now rebuilt a stylesheet architecture from scratch on three different enterprise products — Compito, BMC and Artem — and every time it's for the same underlying reason: the previous approach let ::ng-deep and one-off overrides accumulate until nobody could confidently change a shared style without checking six unrelated screens first. The 7-1 pattern isn't novel, but doing it with real discipline is rarer than it should be.",sections:[{heading:"What actually breaks in enterprise stylesheets",paragraphs:["It's rarely one catastrophic decision. It's a hundred small ones: a developer under deadline pressure reaches for ::ng-deep to override a third-party component's internal class, instead of using the library's documented theming API. Multiply that by a few years and a few team rotations, and you get a stylesheet where every change is guesswork.","The fix isn't a better naming convention — it's removing the escape hatches that let the shortcuts happen in the first place, and giving developers a faster correct path than the incorrect one."]},{heading:"The 7-1 pattern, and where it earns its keep",paragraphs:["7-1 means seven folders, one entry file: abstracts (variables, functions, mixins — no output), base (resets, typography), components, layout, pages, themes, and vendors (third-party overrides, isolated), all pulled together through a single main.scss.","The folder that matters most in an enterprise app is vendors. Every PrimeNG or Angular Material override lives there, scoped and documented, instead of scattered through component-level stylesheets wherever someone happened to need it. When PrimeNG ships a breaking theme change, you know exactly which one folder to check."],bullets:["abstracts/ — design tokens as SCSS variables and mixins, never raw output","vendors/ — every third-party component override, one file per library, documented with why","themes/ — light/dark token maps, nothing else","No file outside abstracts/ and vendors/ is allowed to reference a third-party component's internal class names"]},{heading:"Eliminating ::ng-deep is a policy, not a refactor",paragraphs:["You can't grep-and-replace your way out of ::ng-deep usage, because each instance exists to solve a real visual problem someone had. The actual fix is re-solving each one using the component library's supported theming API — PrimeUIX design tokens, CSS custom properties, ViewEncapsulation.None scoped correctly — and it takes as long as it takes.","What made it stick, across every codebase I've done this on, was pairing the removal with a lint rule or PR-template checklist that flags new ::ng-deep usage before merge. Without that guardrail, the discipline erodes again within a quarter."]},{heading:"Light and dark mode without duplicating every rule",paragraphs:["The pattern I keep coming back to: components only ever reference semantic tokens (--text-heading, --bg-surface, --border-focus), never raw color values, and the themes/ folder is the only place those tokens get redefined per mode. That's the difference between a five-minute dark-mode toggle and a multi-day retrofit — it has to be designed in from the first component, not bolted on after fifty screens already hardcode hex values."]}],takeaway:"A 7-1 architecture is only as good as the rules you enforce around it — the folder structure alone doesn't stop a shortcut under deadline pressure; a documented vendors/ layer and a guardrail against new ::ng-deep usage are what actually make it survive contact with a real team over real years."};export{e as scssArchitectureThatSurvivesTeams};
